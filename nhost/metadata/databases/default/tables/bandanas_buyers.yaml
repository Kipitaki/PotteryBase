table:
  name: buyers
  schema: bandanas
array_relationships:
  - name: buyer_addresses
    using:
      foreign_key_constraint_on:
        column: buyer_id
        table:
          name: buyer_addresses
          schema: bandanas
  - name: orders
    using:
      foreign_key_constraint_on:
        column: buyer_id
        table:
          name: orders
          schema: bandanas
